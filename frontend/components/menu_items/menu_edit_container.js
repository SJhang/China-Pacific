import { connect } from 'react-redux';
import MenuEdit from './menu_edit_form';

const mapStateToProps = () => {

};

const mapDispatchToProps = () => {

};

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(MenuEdit);
